.drop-down-menu
	position relative
	.btn
		border-radius 50%
	.icon
		transform scale(1.3)
		margin 5px
	
	.menu
		position absolute
		right 0
		opacity 0
		transition all 0.3s linear
		transform translateY(5px)
	.visible
		opacity 1